#Input n elements
#Base case: If n < 2, then return the element
#Else:
#Sort the left half of the array (assuming n > 1)
#Sort the right half of the array (assuming n > 1)
#Merge the two halves together

def merge_sort(array)
  if array.length < 2
    return array
  else
  end
end